// Copyright (c) 2026 midnightBITS
// This code is licensed under MIT license (see LICENSE for details)

// THIS FILE IS AUTOGENERATED. DO NOT MODIFY

#include <quick_dra/models/project_reader.hpp>
#include <span>

namespace quick_dra::v{{version}}::partial {
{{# interfaces}}
	bool {{name}}::read(ref_ctx const& ref) {
{{#ext_attrs.versioned}}
		unsigned selector{};
		ref.ignore_errors(true);
		if (!read_key(ref, "{{.}}", selector)) selector = std::numeric_limits<unsigned>::max();
		ref.ignore_errors(false);

		switch(selector) {
{{#versions}}
{{^current}}
			case {{value}}: {
				v{{value}}::partial::{{name}} v{{value}}_{{name}}{};
				if (!v{{value}}_{{name}}.read_v{{value}}(ref)) {
					return false;
				}
				upgrade(std::move(v{{value}}_{{name}}), *this);
				return true;
			}
{{/current}}
{{#current}}
			case std::numeric_limits<unsigned>::max():
			case {{value}}:
				return read_v{{value}}(ref);
{{/current}}
{{/versions}}
			default:
				break;
		}
		return ref.error("unrecognized version when reading `{{.}}`");
	}

	bool {{name}}::read_v{{version}}(ref_ctx const& ref) {
{{/ext_attrs.versioned}}
{{#inheritance}}
		if (!{{{.}}}::read(ref)) return false;

{{/inheritance}}
{{# attributes}}
		if (!read_key(ref, "{{> yaml_name}}", {{name}})) return ref.error("while reading `{{> yaml_name}}`");
{{/ attributes}}

{{#ext_attrs.postprocess}}
		if (!postprocess()) {
			return false;
		}

{{/ext_attrs.postprocess}}
		return true;
	}

	void {{name}}::prepare_for_write() {
{{#ext_attrs.postprocess}}
		preprocess();

{{/ext_attrs.postprocess}}
{{#inheritance}}
		{{{.}}}::prepare_for_write();
{{/inheritance}}
{{# attributes}}
		prepare_child({{name}});
{{/ attributes}}
	}

	void {{name}}::write(ryml::NodeRef& ref) const {
{{#ext_attrs.versioned}}
		switch(version.value_or({{version}}u)) {
{{#versions}}
{{^current}}
			case {{value}}: {
				v{{value}}::partial::{{name}} v{{value}}_{{name}}{};
				downgrade(*this, v{{value}}_{{name}});
				return v{{value}}_{{name}}.write_v{{value}}(ref);
			}
{{/current}}
{{#current}}
			default:
			case {{value}}:
				return write_v{{value}}(ref);
{{/current}}
{{/versions}}
		}
	}

	void {{name}}::write_v{{version}}(ryml::NodeRef& ref) const {
{{/ext_attrs.versioned}}
{{#ext_attrs.schema}}
		write_key(ref, "$schema", "{{{.}}}"s);
{{/ext_attrs.schema}}
{{#inheritance}}
		{{{.}}}::write(ref);
{{/inheritance}}
{{# attributes}}
		write_key(ref, "{{> yaml_name}}", {{name}});
{{/ attributes}}
	}

{{/ interfaces}}
} // namespace quick_dra::v{{version}}
